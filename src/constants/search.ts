/** Search configuration */
export const SEARCH = {
  /** Max results returned per entity type in global search */
  MAX_RESULTS_PER_TYPE: 10,
  /** Debounce delay for search inputs (ms) */
  DEBOUNCE_MS: 300,
} as const
